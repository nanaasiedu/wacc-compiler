# Expected:

begin
  class Coordinate open
    int x,
    int y,
    bool complex
  close

  int a = 5;

  Coordinate c = new Coordinate(2,10,true);
  int pointX = a + c.x;
  int pointY = a + c.y;

  if !c.complex then
    println "not complex"
  else
    println "is complex"
  fi;

  print "x = "; println pointX;
  print "y = "; println pointY

end
